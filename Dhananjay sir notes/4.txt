Spring Data JPA- this dependency allows for the database connectivity
MySQL Driver - Allows for connecting with MySql
table
1. first create a table and insert some record into it.
student
	rollno (primary key)
	name
	course
	branch

create table in st2026 database and insert 5 records into it.
sql
create table student
(rollno numeric(10) primary key,name varchar(60), course varchar(80), branch varchar(120))

insert into student values(105,'Pranjal', 'PhD', 'CS')

2. in application.properties file write the below code-

spring.datasource.url=jdbc:mysql://localhost:3306/st2026?useSSL=false
spring.datasource.username=root
#spring.datasource.password=

3. Create following Packages
	i. Controller
	ii. Entity
	iii. Repository
	iv. Service
4. Inside the entity class create the ORM with Entity class-

package com.training.Entity;

import jakarta.persistence.Entity;
import jakarta.persistence.Id;

@Entity
public class student {

	@Id
	int rollno;
	String name;
	String course;
	String branch;
	public student() {
		// TODO Auto-generated constructor stub
	}
	public student(int rollno, String name, String course, String branch) {
		super();
		this.rollno = rollno;
		this.name = name;
		this.course = course;
		this.branch = branch;
	}
	public int getRollno() {
		return rollno;
	}
	public String getName() {
		return name;
	}
	public String getCourse() {
		return course;
	}
	public String getBranch() {
		return branch;
	}
	public void setRollno(int rollno) {
		this.rollno = rollno;
	}
	public void setName(String name) {
		this.name = name;
	}
	public void setCourse(String course) {
		this.course = course;
	}
	public void setBranch(String branch) {
		this.branch = branch;
	}
	
}

5. Now create the Repository interface inside the repository package.

package com.training.Repository;

import org.springframework.data.repository.CrudRepository;
import org.springframework.stereotype.Repository;

import com.training.Entity.student;

@Repository
public interface StudentRepository extends CrudRepository<student, Integer> {

}

6. Now create a Service class inside the service package

package com.training.Service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.training.Entity.student;
import com.training.Repository.StudentRepository;

@Service
public class StudentService {

	@Autowired
	StudentRepository sturepo;
	
	public Iterable<student> showall()
	{
		return sturepo.findAll();
	}
}


7. Now Finally Create the Controller.

package com.training.Controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import com.training.Entity.student;
import com.training.Service.StudentService;

@RestController
public class AppController {

	@Autowired
	StudentService stuserv;
	@GetMapping("/showrecords")
	public Iterable<student> showrecords()
	{
		return stuserv.showall();
	}
}

Search API

inside service

public Optional<student> searchbyrollno(int rollno)
	{
		return sturepo.findById(rollno);
	}

inside Controller

@GetMapping("/searchrecord/{rollno}")
	public String searchrecord(@PathVariable int rollno)
	{
		String res="";
		Optional<student> rec=stuserv.searchbyrollno(rollno);
		if(rec.isPresent())
		{
			student s=rec.get();
			res+="Roll Number :"+s.getRollno()+"<br>";
			res+="Name :"+s.getName()+"<br>";
			res+="Course :"+s.getCourse()+"<br>";
			res+="Branch :"+s.getBranch()+"<br>";
			return res;
		}
		else
		{
			res="No Record Found";
			return res;
		}
	}
}

