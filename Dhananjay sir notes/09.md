# Record Delete

inside Service

```java
public void deletedata(int rollno)
	{
		sturepo.deleteById(rollno);
	}
```

inside thymeleaf page

```html
<td>
	<a onclick="return confirm('Are you Sure')" th:href="@{deleteapi/{rollno}(rollno=${row.rollno})}">
		<span class="glyphicon glyphicon-remove"></span>
	</a>	
</td>
```

Inside Controller

```java
@GetMapping("/deleteapi/{rollno}")
	public String deleteapi(Model model,@PathVariable int rollno)
	{
		stuserv.deletedata(rollno);
		model.addAttribute("records",stuserv.showall());
		return "showrecords";
	}
```
______________________________________________________________________________

# Record Update

Service
```java
public Optional<student> searchdata(int rollno)
```
{
```
	return sturepo.findById(rollno);
```
}

Controller

```java
@GetMapping("/updatereq/{rollno}")
	public String updatereq(Model model,@PathVariable int rollno)
	{
		Optional<student> obj=stuserv.searchdata(rollno);
		student s=obj.get();
		model.addAttribute("rollno",s.getRollno());
		model.addAttribute("name",s.getName());
		model.addAttribute("course",s.getCourse());
		model.addAttribute("branch",s.getBranch());
		return "frmupdate";
	}
```

# Thymeleaf
showrecords


```html
<td>
					<a th:href="@{updatereq/{rollno}(rollno=${row.rollno})}">
						<span class="glyphicon glyphicon-pencil"></span>
					</a>
				</td>
```

frmupdate
```html
<html xmlns:th="http://www.thymeleaf.org">
	<body>
		<h1>Record Update Window</h1>
		<form method="post" th:action="@{/updateres}">
			<table width="400px" >
				<tr>
					<td>Roll Number</td>
					<td><input type="text" name="t1" th:value="${rollno}" required readonly /></td>
				</tr>
				<tr>
						<td>Name</td>
						<td><input type="text" name="t2" th:value="${name}" required /></td>
				</tr>
				<tr>
						<td>Course</td>
						<td>
							<input type="text" name="t3" th:value="${course}" required />
						</td>
				</tr>
				<tr>
					<td>Select Branch</td>
					<td>
						<input type="text" name="t4" th:value="${branch}" required />
					</td>
				</tr>
```

```html
				<tr>
					<td></td>
					<td>
						<input type="submit" value="Update" name="b1" />
					</td>
				</tr>	
			</table>
		</form>
	</body>
</html>
```

# Search API

# Thymeleaf
```html
	<td>
					<form method="get" name="f1" th:action="@{/searchapi}">
					<input type="text" name="rollno" class="form-control" placeholder="Enter Roll Number" />
				</td>
				<td colspan="2">
					<input type="submit" value="Search" name="btnsearch" class="btn btn-primary" />
					</form>
				</td>
```



```java
@GetMapping("/searchapi")
	public String searchapi(Model model, @RequestParam int rollno)
	{
		Optional<student> obj=stuserv.searchdata(rollno);
		student s=obj.get();
		List<student> records=new ArrayList<>();
		records.add(s);
		model.addAttribute("records",records);
		return "showrecords";
	}
```

