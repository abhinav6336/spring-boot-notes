Request Parameters: They are also used for reading value through API.

@GetMapping("/sayhello")
	public String sayhello(@RequestParam String name,@RequestParam String course)
	{
		String msg="Hello "+name+"<br>";
		msg+="Your Course is "+course;
		return msg;
	}

for calling syntax

	http://localhost:8888/apiname?var1=value&var2=value&.....

example
http://localhost:8888/sayhello?name=Dhananjay&course=PhD

____________________________________________________________________________

Working With Database
	1. MySQL
	2. MongoDB

MySQL
	1. It is a relational database management application
	2. Use SQL for interaction
	3. It is lightweight and open source application
	
SQL Command
1. DDL(Data Definition Language)
2. DML(Data manipulation Language)
3. TCL(Transaction Control Language)
4. DCL (Data Control Language)
5. DQL (Data Query Language)


DDL Command
1. create - creates table etc
Syntax
	create table tablename(fieldname datatype(size),.......................................) 
2. alter- modify the structure of existing table
Syntax
	alter table tablename add columnname datatype(size)
	alter table tablename Modify columnname datatype(size)
	alter table tablename drop column  columnname

3. drop- delete the existing table.
Syntax
	drop table tablename

1. create the below table
emp
	empid number (5)
	ename varchar (40)
	edesig varchar(60)
	salary number (10,2)
2. increase the size of the name field that is 80
3. add a new column named as phone_no varchar(10)


http://localhost/phpmyadmin

create database st2026
use st2026
create table emp(empid numeric(5) primary key,ename varchar(40), edesig varchar(60),salary numeric(10,2)) 
desc emp

DML - Data manipulation Language
1. insert
Syntax
	a. insert into all field 
		insert into tablename(value-1,value-2,.......)
	b. insert into partial field
		insert into tablename(fieldname1,fieldname2,....) values (value-1,value-2)
2. update
3. delete

example
insert into emp values(101, 'Anuj Kapoor', 'Manager',120000)
insert into emp(empid,ename) values(102,'Pankaj Singh')
select * from emp - show all records
Note- insert more 5 records into emp table

Update: update the existing record
Syntax
	update tablename set columnname=value [where condition]
delete : delete the exiting record
Syntax
	delete from tablename  [where condition]

emprec
	empid
	ename
	basic
	ta	(bs*10%)
	da    (bs*15%)
	hra   (bs*6)

create table emprec(empid numeric(5) primary key,ename varchar(60), basic numeric(10),ta numeric(10),da numeric(10), hra numeric(10))

1. insert 5 records in empid,ename and basic fields only
insert into emprec(empid, ename, basic) values(101,'Anuj',70000)
insert into emprec(empid, ename, basic) values(102,'Alok',50000)
insert into emprec(empid, ename, basic) values(103,'Anu',60000)
insert into emprec(empid, ename, basic) values(104,'Amit',30000)
insert into emprec(empid, ename, basic) values(105,'Akash',40000)

2. calculate ta,da and hra using update command
update emprec set ta=basic*10/100,da=basic*15/100,hra=basic*6/100

3. show all the records
select * from emprec





